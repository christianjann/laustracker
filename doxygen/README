The API documentation is online at:

  http://liblaustracker.jann.cc

You can generate a local version with:

  cd ../
  ./configure
  make doxygen

To upload a new version of the API documentation:

  ./publish.sh
